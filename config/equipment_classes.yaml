names:
  0: helmet
  1: vest
  2: gloves
  3: goggles
required_defaults: tuple[str, ...] = (
    tuple(v for v in ("helmet", "vest", "goggles") if v in set(class_map.values()))
    if using_custom_model
    else ()